# ecommerce-msb-persona project

This project bus persona uses spring data.

## Running the application in dev mode

You can run your application in dev mode:

mvn clean package -DskipTests

mvn spring-boot:run
